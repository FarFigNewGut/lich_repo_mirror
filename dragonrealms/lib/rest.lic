=begin
author: Crannach
game: dr
version: 0.01

Just puts 'exp' like the built in rest function, but won't terminate if you touch
the keyboard.
=end


# Global State
interval = 120 # seconds

def rest(interval)
  sleep(120)
  put 'exp'
end

while true
  rest(interval)
end