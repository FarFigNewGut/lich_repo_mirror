=begin

Rummage a container for a certain gem bottle/jar.

Example:

;rum sack uncut diamond
;rum cloak emerald


Email: Jara@play.net
=end

container = "#{script.vars[1]}"
gem = "#{script.vars[2]}"
gem2 = "#{script.vars[3]}"

fput "rummage #{container} ingredient #{gem} #{gem2}"