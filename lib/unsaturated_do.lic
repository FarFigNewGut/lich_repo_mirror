=begin
  like pulse_do.lic (which rips off errand.lic) but instead of waiting for a pulse, waits until you are no longer saturated

  e.g. ;unsaturated ;my_bounty_turner_inner_script_with_an_unreasonably_long_name
  
  Author: Xanlin
  Version: 0.1

=end

wait_while { XMLData.mind_text == "saturated" }

do_client(script.vars[0])