while s = get

	if s =~ /put game text you want to react to here|you can put multiple strings by separating them with vertical lines, like this/	
		put "clap"

	elsif s =~ /put some other trigger text here, to do something else/
		put "yawn"
		put "stare"
		
	elsif s =~ /just keep doing it by putting text in elsif statements like this, and commands to send to game in response, below them/
		put "wake"
		put "stand"
		
	end
end

