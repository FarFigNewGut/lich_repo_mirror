=begin
	"hey there blimpy boy, floating in the air so fancy free"
	version: 1
	author: Machtig
	tags: Icemule
	takes you to a random supernode in IMT
=end

supanode = [ 'one', 'two', 'three', 'four' ]
napspot = supanode.at(rand(supanode.length))

if napspot == "one"
	Script.run('go2', '3449')	
elsif napspot == "two"
	Script.run('go2', '2400')	
elsif napspot == "three"
	Script.run('go2', '3428')	
else 
	Script.run('go2', '3365')	
end

fput "hide"