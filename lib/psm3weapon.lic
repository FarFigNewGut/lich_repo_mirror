=begin
	Automates reactive WEAPON attacks.

	author: Giantphang
	game: Gemstone
	tags: WEAPON, PSM3
	version: 0.1

=end

while line = get
    if line =~ /You could use this opportunity to ([A-Z][a-z]+)/
        attack = $1
        fput "weapon " + attack
	end
end
