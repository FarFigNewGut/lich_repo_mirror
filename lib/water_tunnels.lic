=begin
water tunnels
Because getting out of there is a beeyatch
By Gibreficul, by request
=end

fput "go open"
fput "lie"
fput "lean left"
waitfor "Suddenly the tunnel turns into a nearly vertical drop"
fput "lean left"
waitfor "just ahead the tunnel splits left and right"
fput "lean left"
x = XMLData.room_count
wait_until{XMLData.room_count > x}
echo "YOU ARE OUT OF THE SHITTY TUNNELS"
