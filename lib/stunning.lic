=begin
	version: 1.1
	author: Zegres
	tags: rogue,stunman,stun
	activates stunmanuever - defaults to hide but you can enter a different manuever if you prefer
	if you have any problems hit me up at edk_ on discord
	#example: cman stunman flee
=end

manuever = variable[1]

  if (Vars.manuever.nil? or Vars.manuever.empty?)
      manuever = "hide"
	echo "defaulting to hide"
      end

loop {
	waitrt?
	if checkstunned == true and checkhidden == false
		wait_until{checkstamina > 20 }
		pause_script "ecleanse"
		fput "cman stunman #{manuever}"
			line = waitfor "You attempt to blend", "You're not in any"
   			waitrt?
    			if line =~ /You're not in any/
			  fput "cman stunman flee"  
			  pause 20
    			  end
 			else
   			  end
  		unpause_script "ecleanse"
	pause 1
}
