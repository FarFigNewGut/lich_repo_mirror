while line = get
	if line =~ /^You feel a threatening dark force very close to you../
	if Script.running?("bigshot"); pause_script('bigshot'); end
	waitrt?
	waitcastrt?
	fput "symbol of return"
	echo "THAT WAS CLOSE"
end