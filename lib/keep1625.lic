=begin

	infuse 1625 if it wears off

	author: Hailye

=end


silence_me
loop {
	waitfor "violet flames surrounding it are extinguished"
	if Char.mana > 30
	pause_script("bigshot") if running?("bigshot")
	waitrt
	waitrt?	
	waitcastrt? 
	fput "prep 110"
	fput "infuse my #{checkright}"
		sleep 0.2
		waitrt?
		waitcastrt?
		sleep 0.2
	fput "beseech my #{checkright} conserve"
	unpause_script("bigshot") if running?("bigshot")
end
}
