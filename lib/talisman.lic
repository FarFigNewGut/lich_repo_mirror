

loop{
	waitrt?
	waitcastrt?
	if !Effects::Cooldowns.active?("Twisted Talisman") && GameObj.targets.size.to_i > 0
		talis_target = GameObj.targets.sample 
		put "target ##{talis_target.id}"
		Spell[311].force_incant() if mana > 11
		waitrt?
		waitcastrt?
		fput "point my twisted talisman at ##{talis_target.id}" 
	end
	sleep 1
}