hide_me

while (line = get)
	if line =~ /([A-Z][a-z]+) whispers, "Heal me."/
	target = $1
	start_script('heal', [target])
	end
end

#<pushStream id="thoughts"/>[LNet]-GSIV:Ivurus: "it doesn't like to be pressed"
#  if line =~ /\<pushStream id="death"\/\> .+? noun="([A-Z][a-z]+)".+? just bit the dust!/
#[tayreheal]-GSIV:Jeramin: "meow"