=begin
Author: Demmit
Does: Quits after a Dreavening
=end

while line = get
#Dreaven whispers, "You're all set!"
	if line =~ /^Dreaven exclaims\, \"That\'s everything\!\"$/
		Script.start("rested", "ignore")
		exit
	elsif line =~ /^Dreaven whispers\, \"You\'re all set\!\"$/
		Script.start("rested", "ignore")
		exit
	end
end