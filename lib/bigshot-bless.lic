=begin

	uses symbol of blessing if your bless wears off

	author: Veska

=end

silence_me
loop {
	waitfor "has no effect"
	pause_script("bigshot") if running?("bigshot")
	fput "sym bless my #{checkright}"
	unpause_script("bigshot") if running?("bigshot")
}
